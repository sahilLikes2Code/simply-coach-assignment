<div class="flex justify-between w-full flex-wrap">
  <div class="w-5/6 text-left <%= task.completed? ? 'line-through' : ''  %>">
    <%= task.name %>
  </div>
  <%= render 'tasks/colorised_due_date', task: task %>
</div>
